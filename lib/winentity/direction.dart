import 'package:windesign/helpers/enumvalues.dart';

enum Direction {
  horizontal,
  vertical,
}

final directionValues = EnumValues(
    {"vertical": Direction.vertical, "horizontal": Direction.horizontal});
